# Marketplace Imports

Place user- or platform-provided JSON/CSV inventory exports here. Reference
them from a source manifest with a path such as `imports/example_house.csv`.

These data files are local runtime inputs and should not be committed. Keep
only this README in Git.
